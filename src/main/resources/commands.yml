commands:
  effects:
    status-on: "%p &fPower enabled!"
    status-off: "%p &fPower disabled!"

    converted: "%p &fPower converted to a potion."

    potion-message: "&8->%newline%&8- &fList of potions:%newline%%value%%newline%&8->"

  effectranks:
    help:
      - "%p &fPlugin help 1/1"
      - "&f/effects [on/off/convert] &8: &fTo enable/disable/convert the power."
      - "&f/effectranks &8: &fThe plugin manager."
    load: "%p &fReloading plugin..."
    load-file: "%p &fReloading file.."
    reload: "%p &fPlugin reloaded!"
    reload-file: "%p &f%file% &freloaded!"


